# Support Tests

This folder contains the non-runtime test suite for `SA-NOM_AI_Director`.

The tests were moved out of the root to keep the main project surface focused on:
- product runtime files
- PTAG and role packs
- deployment files
- active planning and roadmap documents

The review pack builder still includes these tests in the curated `_review` output.
