# Runtime State

`_runtime/` stores generated local state such as access profiles, audit logs, session stores, evidence exports, and smoke reports.

This directory should not be treated as source code.
For public repositories, keep only placeholders such as `.gitkeep` and use the files in `examples/` when you need sanitized sample artifacts.
