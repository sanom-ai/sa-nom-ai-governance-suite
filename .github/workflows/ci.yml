name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  statuses: write

jobs:
  verify:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.14']
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: |
            requirements-dev.txt
            pyproject.toml

      - name: Install development dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt

      - name: Run lightweight security audit (advisory)
        id: security_audit
        continue-on-error: true
        run: python -m pip_audit -r requirements-dev.txt

      - name: Publish security audit summary
        if: always()
        run: |
          if [ "${{ steps.security_audit.outcome }}" = "success" ]; then
            echo "## Security Audit" >> "$GITHUB_STEP_SUMMARY"
            echo "pip-audit completed with no blocking findings." >> "$GITHUB_STEP_SUMMARY"
          else
            echo "## Security Audit" >> "$GITHUB_STEP_SUMMARY"
            echo "pip-audit reported findings (advisory, non-blocking)." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Lint Python sources
        run: python -m ruff check .

      - name: Lint core modules with strict rule set
        run: python -m ruff check sa_nom_governance/core --select E,F,I --ignore E501

      - name: Run static type checks
        run: python -m mypy

      - name: Compile Python sources
        run: python -m compileall -q .

      - name: Run test suite with coverage
        run: python -m pytest _support/tests

      - name: Enforce core module coverage floor
        run: python scripts/check_core_coverage.py coverage.xml 82

      - name: Enforce runtime governance coverage floor
        run: python scripts/check_runtime_governance_coverage.py coverage.xml 85

      - name: Enforce enterprise runtime coverage floor
        run: python scripts/check_enterprise_runtime_coverage.py coverage.xml 84

      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: coverage-xml-${{ matrix.python-version }}
          path: coverage.xml
          if-no-files-found: error


  runtime_governance_verify:
    name: runtime-governance-verify
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.14'
          cache: pip
          cache-dependency-path: |
            requirements-dev.txt
            pyproject.toml

      - name: Install development dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt

      - name: Run governed runtime matrix tests
        run: python -m pytest --no-cov _support/tests/test_runtime_governance_matrix.py _support/tests/test_decision_flow.py _support/tests/test_audit_integrity.py -q

      - name: Publish governed runtime summary
        if: always()
        run: |
          echo "## Governed Runtime Verify" >> "$GITHUB_STEP_SUMMARY"
          echo "Focused authority/policy/evidence matrix checks completed for governed runtime paths." >> "$GITHUB_STEP_SUMMARY"


  enterprise_runtime_verify:
    name: enterprise-runtime-verify
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.14'
          cache: pip
          cache-dependency-path: |
            requirements-dev.txt
            pyproject.toml

      - name: Install development dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt

      - name: Run enterprise runtime validation cluster
        run: >-
          python -m pytest --no-cov
          _support/tests/test_enterprise_validation_suite.py
          _support/tests/test_workflow_proof_artifacts.py
          _support/tests/test_runtime_recovery.py
          _support/tests/test_operational_readiness.py
          _support/tests/test_workflow_state_store.py
          -q

      - name: Publish enterprise runtime summary
        if: always()
        run: |
          echo "## Enterprise Runtime Verify" >> "$GITHUB_STEP_SUMMARY"
          echo "Focused validation, proof, readiness, workflow-state, and recovery checks completed for enterprise runtime paths." >> "$GITHUB_STEP_SUMMARY"


  pt_oss_advisory:
    name: pt-oss-advisory
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.14'
          cache: pip
          cache-dependency-path: |
            requirements-dev.txt
            pyproject.toml

      - name: Install development dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt

      - name: Run PT-OSS advisory check (non-blocking)
        id: ptoss
        continue-on-error: true
        run: python -m pytest --no-cov _support/tests/test_pt_oss_engine.py -q

      - name: Publish PT-OSS advisory summary
        if: always()
        run: |
          if [ "${{ steps.ptoss.outcome }}" = "success" ]; then
            python scripts/pt_oss_advisory_report.py >> "$GITHUB_STEP_SUMMARY"
          else
            echo "## PT-OSS Advisory" >> "$GITHUB_STEP_SUMMARY"
            echo "PT-OSS advisory check reported findings or execution issues (non-blocking)." >> "$GITHUB_STEP_SUMMARY"
          fi


  scripts_lint_advisory:
    name: scripts-lint-advisory
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.14'
          cache: pip
          cache-dependency-path: |
            requirements-dev.txt
            pyproject.toml

      - name: Install development dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-dev.txt

      - name: Run scripts lint advisory (non-blocking)
        id: scriptslint
        continue-on-error: true
        run: python -m ruff check scripts --select E402,F403

      - name: Publish scripts lint advisory summary
        if: always()
        run: |
          if [ "${{ steps.scriptslint.outcome }}" = "success" ]; then
            echo "## Scripts Lint Advisory" >> "$GITHUB_STEP_SUMMARY"
            echo "scripts/ wrapper lint advisory passed." >> "$GITHUB_STEP_SUMMARY"
          else
            echo "## Scripts Lint Advisory" >> "$GITHUB_STEP_SUMMARY"
            echo "scripts/ wrapper lint advisory found debt (non-blocking)." >> "$GITHUB_STEP_SUMMARY"
          fi


  publish_verify_status:
    name: publish-legacy-verify-status
    runs-on: ubuntu-latest
    needs: verify
    if: always()
    steps:
      - name: Publish legacy verify status context
        uses: actions/github-script@v7
        with:
          script: |
            const state = '${{ needs.verify.result }}' === 'success' ? 'success' : 'failure';
            const sha = context.eventName === 'pull_request'
              ? context.payload.pull_request.head.sha
              : context.sha;
            await github.rest.repos.createCommitStatus({
              owner: context.repo.owner,
              repo: context.repo.repo,
              sha,
              state,
              context: 'verify',
              description: state === 'success'
                ? 'Legacy verify compatibility status published.'
                : 'Legacy verify compatibility status failed.',
              target_url: `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
            });
